# Production environment variables for Render deployment
# The API URL should be relative (same domain) - backend serves both frontend and API
# Leave empty to use relative URLs, or set to empty string to use same origin
REACT_APP_API_URL=
